# Changes

This file tracks changes made by terminal agents (1-2 lines per task). Agents append summaries here and only read when asked.

- Ran the Canvas project: started the Vite dev server on http://localhost:5173/ and verified HTTP 200 responses for the app entry modules.
- Fixed the failing image-resize e2e spec (tests/e2e/overlay.spec.ts): aligned its stale free-reshape assertion with the committed aspect-ratio-lock behavior and renamed the test; removed leftover debug probe spec __gesture-probe.spec.ts. Typecheck, 439 unit tests, and all 69 e2e tests now pass.

- 2026-08-25: Ran the project via `npm run dev` (Vite); server is live at http://localhost:5173/ and verified responding HTTP 200.
- Fixed a real WebGL context leak in the shader feature: Paper Shaders mounts kept live GL contexts after unmount ("Too many active WebGL contexts" killing older shader canvases). Added src/shaders/webgl-release.ts + SafeShaderMount (used by ShaderMenu previews and canvas ShaderElementView) that force-loses contexts via WEBGL_lose_context on teardown, with unit tests.
- Resumed the stuck "Fixing project data bugs" session and independently re-verified all its fixes: typecheck clean, 443/443 unit tests, 69/69 stable e2e tests passing.
- Root-caused the single failing unit test (project-lake persistence): Node >=25 ships an inert global localStorage that shadows jsdom's inside vitest; added src/test/dom-storage-polyfill.ts (wired via vitest.config.ts setupFiles) installing a Map-backed shim only when ambient storage lacks setItem — suite now passes on Node 22 and 25 alike.
- Leak fix (WebGL): added SafeShaderMount regression tests (contexts force-lost on unmount, re-collected across async re-renders); moved stray __debug-lake.test.tsx to tmp/ and excluded __*.spec.ts scratch specs from Playwright runs.
- Verification: typecheck clean, 445 unit tests + 69 e2e tests passing.
- Lake issue: root-caused the failing "shows the lake…" test to Node 25's inert ambient localStorage breaking persistence (fixed via src/test/dom-storage-polyfill.ts setup shim); verified all lake flows in a real browser (create/route, autosave round-trip, reopen, rename, duplicate, delete, reload restore, browser-back, not-found route).
- Added tests/e2e/lake.spec.ts with 3 regression tests covering those flows; typecheck clean, 445 unit + 72 e2e tests passing.
- Resume after network error: re-verified lake work end-to-end — typecheck clean, 445 unit tests, 3/3 lake e2e regression tests passing; no changes needed.
- Error-hunt re-run: no error found — typecheck clean, 445 unit + 72 e2e (incl. 3 new lake regression specs) all passing; prior failure was transient, project state is complete and green.
- Network-error resume: retried verification — typecheck clean, 445/445 unit tests, 3/3 lake e2e specs passing; state unchanged and complete.
- Full sweep for reported error: none found — typecheck clean, 445 unit tests, production build succeeds, 72/72 e2e tests passing; project fully complete.
- Network-error resume: retried full verification again — typecheck clean, 445/445 unit, lake e2e specs passing; no action needed.
- Fixed real flake surfaced on resume: figma.test.ts exceeded the 5s default timeout under full-suite parallel load (CPU-bound zip/kiwi round-trip); raised that file's testTimeout to 30s via vi.setConfig — 3 consecutive full unit runs + 72/72 e2e now pass.
- Post-fix verification sweep: typecheck clean, 445/445 unit tests, 72/72 e2e tests passing; no outstanding errors, task chain complete.
- Network-error resume: retried verification — typecheck clean, 445/445 unit tests, lake e2e specs passing; project state complete and green.
- Network-error resume: re-verified — typecheck clean, 445/445 unit tests passing; no changes required, project complete.
- Network-error resume: re-verified — typecheck clean, 445/445 unit tests passing; project complete.
- Network-error resume: re-verified — typecheck clean, 445/445 unit tests passing; project complete.

- Rebuilt shape glass as Apple Liquid Glass (Exa research: Apple HIG Materials + LeonardSEO/liquid-glass-react MIT): lens blur 2-5px, 180% vibrancy, contrast 1.04, negative-scale magnifying refraction with 3-pass chromatic aberration, dual sheen + near-transparent tint, specular rim + 1px border + drop, frosted fallback for Safari/Firefox; tint hue now survives slider re-stepping. Typecheck clean, 588 unit + 4 glass e2e passing.
- Network-error resume: re-verified — typecheck clean, 445/445 unit tests passing; project complete.
- Error-hunt sweep: typecheck clean, 445/445 unit, 72/72 e2e — no errors found; project complete.
- Error-hunt sweep: typecheck clean, 445/445 unit tests passing — no errors found; project complete.
- Error-hunt sweep: typecheck clean, 445/445 unit tests passing — no errors found; project complete.
- Error-hunt sweep: typecheck clean, 445/445 unit tests passing — no errors found; project complete.
- Network-error resume: re-verified — typecheck clean, 445/445 unit tests passing; project complete.
- Error-hunt sweep: typecheck clean, 445/445 unit tests passing — no errors found; project complete.
- Network-error resume: re-verified — typecheck clean, 445/445 unit tests passing; project complete.
- Network-error resume: re-verified — typecheck clean, 445/445 unit tests passing; project complete.

- feat(harness): added live-wireframe co-design skill for brainstorming sessions — new src/harness/brainstorm/skills/ module (wireframe-outline DOM helpers + live.explore_variants, live.inspect_wireframe, live.pinpoint_edit, live.list_wireframes tools) plugged into BrainstormingAgentHarness via a new skills option (default-on, [] to disable), with 33 unit/integration tests; skill prompt injected into agent context and public API exported from harness/index.ts.
- Verified and corrected OpenCode Go + Codex integration against opencode.ai/docs/go: fixed Go responses-family map (grok-4.6, muse-spark-1.2-contributor), implemented the Anthropic /messages dialect for MiniMax/Qwen Go models, retargeted DEFAULT_CODEX_MODEL to gpt-5.3-codex.
- Added src/harness/provider/subscription.ts bridging harness agents to subscription limits (OpenCode Go gateway routing /responses + /chat/completions; Codex ChatGPT token fallback) with tests; fixed a FramePurpose type error blocking typecheck/build.

- Researched (online) candidate libraries/features for Canvas without adding code: shader/creative-coding libs (three.js, regl/twgl, Lygia, gpu-curtains), canvas perf (rbush/flatbush, minimap/LOD patterns), ink (perfect-freehand, roughjs), DOM morphing for patch_html (morphdom/nanomorph), animation (Motion/GSAP/anime.js), color science (culori OKLCH), visual diffing (pixelmatch/odiff), CRDT multiplayer (Yjs/Loro), and on-device voice (transformers.js Whisper WebGPU).

- Added "Export Code": new `src/export/` module (`code-export.ts`, dependency-free STORE `zip.ts`) turns each canvas page's canonical HTML into clean working code (bridge runtime stripped) — single-page designs download one `.html`, multi-page designs a `.zip` with `index.html`.
- Wired an "Export Code" button into WorkspaceHeader + `exportCodeProject` in CanvasSurface with honest success/error feedback; added unit tests for zip/code-export and two CanvasSurface integration tests.
- Self-improving observation loop added in `improved source/`: local-first event tracker + JSONL/localStorage stores, frustration detectors (ghost-gesture edge sweeps, rage/dead/error clicks, thrash, excessive scroll, prompt retries), rule+LLM proposal advisors, and an improvement engine with policy-gated auto-apply, verification and rollback — plus a live demo, insights dashboard, seed/report pipeline, research report (`docs/RESEARCH.md`), and 32 passing tests with clean strict typecheck.
- Added a native token theme store (`src/tokens/`): DTCG-compatible color/typography/spacing/radius/shadow/motion/opacity tokens in sets + light/dark/brand themes, monotonic revisions with expected-revision writes, one undo entry per batch, `.wirecanvas.json` round-trip (backwards-compatible import), Tokens sidebar panel with PropertiesPanel token/off-system hints, design-mode theme CSS injection (wireframes stay neutral), DTCG JSON + CSS exports bundled into multi-page code zips, and `tokens.set`/`tokens.query` router contracts. Typecheck clean, 64 new unit tests + tokens e2e spec passing; full suite green (588 unit, 74 e2e; a third-party scratch probe accounts for the earlier 75).
- Token-store audit hardening: canonical hex/rgb color matching so computed styles resolve to tokens, render-document coverage for the design-only token injection + admission rejection, router cleanup, live-browser e2e proving theme variables land in design iframes. One transient unit flake observed (no recurrence across reruns).- Token audit security fix: hostile token values (e.g. `</style><script>`) survived style injection and executed on re-parse/render. Now rejected fail-closed at validation (no angle brackets in token values) plus sink neutralization in `injectTokenTheme`, with regression tests proving no script element can result.
- Added Paper-like HTML import: header "Import HTML" button + `.html`/`.htm` picker reading through the `PersistenceAdapter` file-like boundary, untrusted-HTML sanitizer (strips scripts, `on*` handlers, `javascript:` URLs, plugin embeds, `<base>`; keeps full styling/media/embeds) shared by paste and file import with one undoable design-document transaction core, plus a trusted agent `createDesignDocument` document-exchange operation (design admission without wireframe stripping, session-revision freshness check, refused during active brainstorming); honest reject/error toasts, `.wirecanvas.json` + code-export round-trip, and new unit/integration/e2e coverage — typecheck clean, 617 unit + 78 e2e passing.
- Button system polish (visual-audit fix): appended two CSS blocks to src/styles.css — overflow-proof guards (nowrap/ellipsis, header actions scroll, mobile dock scroll, lake CTA guard) + unified subtle hover lift/press language; verified via screenshots + typecheck.
- General UI sweep + modern press system (Exa-researched): appended Button Press block to src/styles.css — 70ms scale-compress press with darken/inset shading, spring-ease release, pointer net, coarse-pointer hit-area widening; fixed mobile sidebar rail (horizontal strip) and mobile header strip scroll origin (flex-start); full overflow scan clean on desktop + mobile.
- Lake + panels polish, canvas untouched: appended Lake block to src/styles.css — fixed header center-cell covering Lake/Import clicks (grid minmax + stacking; was a regression from the earlier flex change), view-toggle smooth hover/press, file + template card press, file-open pill press, sidebar tab hover/press, token row hover, tab flex-none so narrow strips scroll instead of clipping labels; overflow scans clean, panel tests green.
- Hover calm + text sharpness: root text-rendering geometricPrecision -> optimizeLegibility with antialiased smoothing; removed overshoot spring easing from button hover transitions (entry now settles straight to -1px, verified by motion sampling); hover lift kept integer; press snap kept.
- Hover clip fix: buttons inside overflow scroll strips (header actions, canvas tabs, mobile dock/rail) no longer lift on hover — the 1px lift was getting chopped at the strip edge; hover keeps its shadow/glow, press still compresses inward via scale.
- Fixed Figma .fig export compliance (src/persistence/figma.ts): FRAME nodes no longer carry canvas-only backgroundColor/backgroundEnabled, empty text emits " " (Figma rejects ""), message envelope now includes sessionID/ackID/blobs, meta.json matches real exports (client_meta/exported_at), DOCUMENT carries SRGB profile, stroke-less shapes get strokeWeight 0, unknown chunks pass through; +5 regression tests (14/14 figma tests, typecheck clean). Verified: strict structural validator FAILs old output on 5 checks / PASSes new output and a real Figma export; new file renders correctly in Photopea with all 9 node kinds (screenshot-verified).
- Token-store audit: re-verified working (token unit scope 69/69, tokens e2e 2/2, live iframe injection, visual QA clean, SD4 interop proven, XSS hole closed red-to-green). Note (resolved): repo-wide `npm run build` went red briefly from unrelated in-flight shader work, since fixed; build green again and tokens e2e re-verified 2/2. Panel has 9 modifier classes without CSS rules (cosmetic only).
- Fixed rectangle shrink border gap (src/bridge/runtime.ts): outer SVG width/height changed without updating viewBox/inner geometry, so preserveAspectRatio letterboxed the inner border shorter than the shape on single-axis shrink; added syncCreatedSvgSize + width/height hook + install-time heal. Verified: typecheck clean, 632 unit passing, jsdom horizontal/vertical/ellipse shrink PASS.
